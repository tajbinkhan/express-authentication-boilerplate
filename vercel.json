{
	"version": 2,
	"builds": [
		{
			"src": "package.json",
			"use": "@vercel/node",
			"config": {
				"buildCommand": "pnpm run build"
			}
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "src/server.cjs"
		}
	]
}
